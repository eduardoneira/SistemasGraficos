Object3D < Mesh
  _M = [][]
  _childs = [Object3D]
  initialize(texture,rows,cols)               
  setUpBuffers                                #position and texture, super 
  draw
  translate -> M = M * Mt
  rotate -> M = M * Mr
  scale -> M = M * Ms

cylynder
  childs = []
  childs << tapa1.new
  tapa1.translate(0,0,z)
  childs << tapa2.new
  tapa2.translate(0,0-z)

  draw
    gl.draw
    for child in childs
      child.draw(M)



main 
  cylyner.new
  cylynder.scale 2
  cylynder.translate 1,1,-1